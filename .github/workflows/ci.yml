# Placeholder workflow — the real ci pipeline is built by the devops-engineer
# in Phase 4 (lint, typecheck, test, build / container push / SBOM + scan).
name: ci

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "ci pipeline lands in Phase 4"
