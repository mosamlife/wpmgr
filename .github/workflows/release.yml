# Placeholder workflow — the real release pipeline is built by the devops-engineer
# in Phase 4 (lint, typecheck, test, build / container push / SBOM + scan).
name: release

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "release pipeline lands in Phase 4"
