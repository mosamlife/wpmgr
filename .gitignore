# ---- Go ----
/apps/api/bin/
/apps/cli/bin/
*.exe
*.test
*.out
coverage.txt
coverage.html
go.work.sum

# ---- Node / pnpm ----
node_modules/
dist/
.turbo/
*.tsbuildinfo
.vite/
playwright-report/
test-results/
.eslintcache

# ---- PHP / Composer ----
/apps/agent/vendor/
composer.lock
.phpunit.result.cache
.phpunit.cache/

# ---- Env / secrets ----
.env
.env.*
!.env.example
*.pem
*.key
!**/testdata/**/*.key
secrets/
keys/

# ---- IDE / OS ----
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# ---- Build artifacts ----
/release/
/sbom/
*.zip
!apps/agent/**/*.zip

# stray compiled binaries (build to bin/ instead)
/apps/api/wpmgr
/apps/api/media-encoder
/wpmgr
/cmd

# ---- Local research / scratch trees (never part of the product) ----
/reference/
/analysis/

# ---- Stray root-level agent release zips (real releases live in /release/) ----
/wpmgr-agent-*.zip

# ---- corpus-gen build artifacts (source files ARE tracked; binaries are not) ----
/tools/corpus-gen/corpus-gen
/tools/corpus-gen/manifest.json

# ---- Skills (installed via npx skills add; lockfile is tracked separately) ----
.agents/
.claude/skills/
.claude/scheduled_tasks.lock

# ---- pnpm content-addressed store (local cache; not source) ----
.pnpm-store/

# ---- Claude Code transient worktrees (never committed) ----
/.claude/worktrees/

# local wp-cli (Plugin Check harness)
wp-cli.phar
wp-cli.phar.asc
docs/security/wporg-t13-*.md
docs/security/wporg-t14-*.md
