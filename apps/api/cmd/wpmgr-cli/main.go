// Command wpmgr-cli is the in-tree administrative CLI for the control plane
// (migrations, key generation, seed). The standalone end-user `wpmgr` CLI
// ships separately in apps/cli (V1). Bootstrap stub for Phase 2.
package main

import "fmt"

func main() {
	fmt.Println("wpmgr-cli — admin tooling (bootstrap)")
}
