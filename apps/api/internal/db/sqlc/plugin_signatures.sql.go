// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: plugin_signatures.sql

package sqlc

import (
	"context"
)

const allPluginSignatures = `-- name: AllPluginSignatures :many
SELECT slug, corpus_version, option_patterns, transient_patterns, table_patterns, cron_hook_patterns, updated_at
FROM plugin_signatures
ORDER BY slug
`

func (q *Queries) AllPluginSignatures(ctx context.Context) ([]PluginSignature, error) {
	rows, err := q.db.Query(ctx, allPluginSignatures)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []PluginSignature
	for rows.Next() {
		var i PluginSignature
		if err := rows.Scan(
			&i.Slug,
			&i.CorpusVersion,
			&i.OptionPatterns,
			&i.TransientPatterns,
			&i.TablePatterns,
			&i.CronHookPatterns,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getPluginSignatureBySlug = `-- name: GetPluginSignatureBySlug :one

SELECT slug, corpus_version, option_patterns, transient_patterns, table_patterns, cron_hook_patterns, updated_at
FROM plugin_signatures
WHERE slug = $1
`

// M40 plugin_signatures queries.
// Global reference data (no tenant_id). The corpus table uses ENABLE RLS with a
// permissive SELECT policy; wpmgr_app has SELECT only. These queries run under the
// normal pool (no tenant GUC needed — any authenticated session may read).
func (q *Queries) GetPluginSignatureBySlug(ctx context.Context, slug string) (PluginSignature, error) {
	row := q.db.QueryRow(ctx, getPluginSignatureBySlug, slug)
	var i PluginSignature
	err := row.Scan(
		&i.Slug,
		&i.CorpusVersion,
		&i.OptionPatterns,
		&i.TransientPatterns,
		&i.TablePatterns,
		&i.CronHookPatterns,
		&i.UpdatedAt,
	)
	return i, err
}
