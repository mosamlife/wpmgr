// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: billing.sql

package sqlc

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const countActiveSitesForBilling = `-- name: CountActiveSitesForBilling :one
SELECT billing_count_active_sites($1)::bigint AS count
`

// Wraps the SECURITY DEFINER billing_count_active_sites() so the count is
// correct under any RLS GUC context the caller happens to be running in
// (operator app.tenant_id OR public-enroll app.enroll) — see the function's
// own comment in schema.sql for the full rationale.
func (q *Queries) CountActiveSitesForBilling(ctx context.Context, tenantID uuid.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, countActiveSitesForBilling, tenantID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const getTenantBilling = `-- name: GetTenantBilling :one
SELECT plan, plan_status, plan_overrides, grace_until, current_period_end
FROM tenants
WHERE id = $1
`

type GetTenantBillingRow struct {
	Plan             string             `json:"plan"`
	PlanStatus       string             `json:"plan_status"`
	PlanOverrides    []byte             `json:"plan_overrides"`
	GraceUntil       pgtype.Timestamptz `json:"grace_until"`
	CurrentPeriodEnd pgtype.Timestamptz `json:"current_period_end"`
}

// Returns the M16 Phase A billing/plan fields used by internal/billing's
// entitlement resolution. tenants carries no RLS (see schema.sql); every
// caller already holds a tenant_id it is entitled to query — either the
// acting tenant's own scope (operator paths) or the tenant_id resolved from a
// verified pairing code (the public /enroll path, InEnrollTx).
func (q *Queries) GetTenantBilling(ctx context.Context, tenantID uuid.UUID) (GetTenantBillingRow, error) {
	row := q.db.QueryRow(ctx, getTenantBilling, tenantID)
	var i GetTenantBillingRow
	err := row.Scan(
		&i.Plan,
		&i.PlanStatus,
		&i.PlanOverrides,
		&i.GraceUntil,
		&i.CurrentPeriodEnd,
	)
	return i, err
}
