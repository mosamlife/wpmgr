// Bootstrap placeholder. The real router, layout, and feature pages are built
// in Phase 4 by the frontend-architect subagent once the frontend ADRs
// (router, data layer, component lib) are locked.
export function App() {
  return (
    <main>
      <h1>WPMgr</h1>
      <p>Control plane bootstrap. Dashboard arrives in Phase 4.</p>
    </main>
  );
}
