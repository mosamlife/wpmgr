# apps/web — WPMgr dashboard (React 19 + Vite)

Vite SPA. Features under `src/features/<domain>/` mirror backend domains.
Full stack (router, data layer, component lib) is locked in Phase 3 ADRs and
built in Phase 4.

```bash
pnpm dev
pnpm build
```
